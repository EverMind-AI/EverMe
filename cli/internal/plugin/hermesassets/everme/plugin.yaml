name: everme
version: 0.1.0
description: "EverMe cross-session memory — native MemoryProvider with automatic per-turn capture and recall, no model-initiated tool calls required."
pip_dependencies: []
hooks:
  - sync_turn
  - on_session_end
  - on_pre_compress
  - on_session_switch
  - on_memory_write
